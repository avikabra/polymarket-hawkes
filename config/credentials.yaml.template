# Copy to credentials.yaml and fill in. Never commit credentials.yaml.
google_cloud:
  project_id: "YOUR_GCP_PROJECT_ID"
  # Set GOOGLE_APPLICATION_CREDENTIALS env var to path of service account JSON
anthropic:
  # Set ANTHROPIC_API_KEY env var — do not paste key here
  api_key_env: "ANTHROPIC_API_KEY"
